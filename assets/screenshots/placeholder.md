<!-- assets/screenshots/placeholder.md -->
# Screenshot

Questa directory conterrà screenshot dell'output della CLI e degli
strumenti di consultazione (es. `kerneltrace-mgmt tail` con
evidenziazione a colori) da includere in `README.md` e nella
documentazione, una volta disponibile un ambiente dimostrativo stabile.

Screenshot pianificati:
- Output di `kerneltrace-cli status`
- Output di `kerneltrace-cli rules list`
- Un evento di detection critico mostrato da `kerneltrace-mgmt tail`